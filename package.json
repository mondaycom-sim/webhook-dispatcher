{
  "name": "@mondaycom/webhook-dispatcher",
  "version": "1.0.0",
  "description": "Webhook dispatcher for monday.com integrations",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && zip -r dist.zip dist/"
  },
  "dependencies": {
    "openai": "^4.28.0",
    "@aws-sdk/client-bedrock-runtime": "^3.500.0",
    "@aws-sdk/client-secrets-manager": "^3.500.0",
    "express": "^4.18.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0"
  }
}
